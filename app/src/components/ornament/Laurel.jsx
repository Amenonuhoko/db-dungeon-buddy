// The laurel branches belonged to the old Greek-ornament look and were
// retired with the Dungeon Buddy rebrand (BIBLE.md §3) — the mascot's
// sticker style carries a title on its own. Kept as a pass-through so
// existing usages don't have to change.
export function LaurelFlourish({ children }) {
  return <div style={{ display: 'flex', justifyContent: 'center' }}>{children}</div>;
}
